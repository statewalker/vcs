/**
 * Tree storage interface
 *
 * Re-exported from @webrun-vcs/core for backwards compatibility.
 */
export type { TreeStore } from "@webrun-vcs/core/stores";

// TreeEntry is also re-exported from types.ts for backwards compatibility
export type { TreeEntry } from "./types.js";
