/**
 * Volatile storage interfaces
 *
 * Re-exports from the core interfaces package.
 * These interfaces define temporary content storage for buffering
 * streaming content to compute size during object creation.
 */

export type { VolatileContent, VolatileStore } from "../../interfaces/volatile-store.js";
