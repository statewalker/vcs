/**
 * Staging area (index) interfaces
 *
 * Re-exported from @webrun-vcs/core for backwards compatibility.
 */
export {
  MergeStage,
  type MergeStageValue,
  type StagingBuilder,
  type StagingEdit,
  type StagingEditor,
  type StagingEntry,
  type StagingEntryOptions,
  type StagingStore,
} from "@webrun-vcs/core/staging";
