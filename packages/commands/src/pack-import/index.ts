/**
 * Pack-import plumbing shared by the commands that receive packs
 * (`clone`, `fetch`).
 *
 * Internal: not re-exported from `src/index.ts`.
 */

export { importPackIntoHistory } from "./pack-import.js";
