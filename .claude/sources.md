# Reference Sources

When analyzing algorithms or patterns, consult these reference implementations:

- **JGit** (Java): Canonical Git implementation, good for pack/delta/transport internals
- **Fossil** (C): Alternative VCS, useful for delta/diff algorithm patterns
- **isomorphic-git** (JS): Browser-compatible Git, useful for API design patterns
